name: 🚀 Run Autotests

on: [push, pull_request]

permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  run-tests:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
    - name: 📥 Checkout code
      uses: actions/checkout@v4

    - name: 🐳 Build Docker image
      run: docker build -t my-autotests .
      timeout-minutes: 8

    - name: 🧪 Run tests
      run: docker run -v $(pwd)/allure-results:/app/allure-results my-autotests
      timeout-minutes: 5

    - name: 📊 Publish Allure Report
      uses: simple-elf/allure-report-action@v1.7
      with:
        allure_results: allure-results
        keep_reports: 5

#name: 🚀 Run Autotests
#
#on: [push, pull_request]
#
#jobs:
#  run-tests:
#    runs-on: ubuntu-latest
#    timeout-minutes: 15
#
#    steps:
#    - name: 📥 Checkout code
#      uses: actions/checkout@v4
#
#    - name: 🐳 Build Docker image
#      run: docker build -t my-autotests .
#      timeout-minutes: 8
#
#    - name: 🧪 Run tests
#      run: docker run -v $(pwd)/allure-results:/app/allure-results my-autotests
#      timeout-minutes: 5
#
#    - name: 📊 Allure Report
#      uses: simple-elf/allure-report-action@v1.7
#      if: always()
#      with:
#        allure_results: allure-results
#        allure_report: allure-report
#        keep_reports: 5
#
#    - name: 📁 Upload Allure Report
#      uses: actions/upload-artifact@v4
#      if: always()
#      with:
#        name: allure-report
#        path: allure-report/
#        retention-days: 30

#name: 🚀 Run Autotests
#
#on: [push, pull_request]
#
#jobs:
#  run-tests:
#    runs-on: ubuntu-latest
#    timeout-minutes: 15  # Общий таймаут
#
#    steps:
#    - name: 📥 Checkout code
#      uses: actions/checkout@v4
#
#    - name: 🐳 Build Docker image
#      run: docker build -t my-autotests .
#      timeout-minutes: 8  # Таймаут на сборку
#
#    - name: 🧪 Run tests
#      run: docker run -v $(pwd)/allure-results:/app/allure-results my-autotests
#      timeout-minutes: 5  # Таймаут на тесты
#
#    - name: 💾 Upload Allure results
#      uses: actions/upload-artifact@v4
#      with:
#        name: allure-results
#        path: allure-results/